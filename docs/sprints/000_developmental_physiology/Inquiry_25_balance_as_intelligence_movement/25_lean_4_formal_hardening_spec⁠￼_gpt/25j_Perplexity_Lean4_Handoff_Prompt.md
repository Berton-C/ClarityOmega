# 25j — Perplexity Lean 4 Handoff Prompt
## Copy/paste this prompt with files 25f, 25g, 25h, and 25i

You are acting as an independent Lean 4 proof engineer and adversarial formalization reviewer for Investigation 25 of the ClarityOmega world-model kernel.

You are being given four governing documents:

1. `25f_Ratified_Balance_as_Intelligence_Mathematical_Object.md`
2. `25g_Lean4_Formal_Hardening_Specification.md`
3. `25h_Lean4_Proof_Obligations_and_Adversarial_Tests.md`
4. `25i_Lean4_Encoding_Decision_Log_and_Gap_Report_Contract.md`

Treat `25f` as the semantic authority.

Treat `25g` and `25h` as the formal-hardening contract.

Treat `25i` as the mandatory reporting discipline.

## Your task

Produce a real Lean 4 project that faithfully formalizes the Investigation-25 object without redesigning it for proof convenience.

### You must:

1. Pin an actual Lean 4 toolchain.
2. Create a self-contained Lake project.
3. Encode the minimal typed signature from `25g`.
4. Keep Soul doctrine ambient; do not store Soul standing as a DPF carrier field.
5. Represent R/A/P as movement-indexed, non-reconstructive readings/profiles, not three scalar coordinates.
6. Encode Investigation-24 legality as an abstract imported/inherited interface rather than reimplementing it.
7. Encode proof-relevant Investigation-25 admissibility.
8. Constrain `ReadApp` by RA1–RA8.
9. Preserve non-compensatory material floors for Relatedness and Precision.
10. Keep ActionEffort and ExcessForcing distinct.
11. Encode HarmonicAlign as a derived relation.
12. Encode Bounded Harmonic Continuation as a coherence/soundness theorem, and prove its operational non-harmonic falsifier if logically derivable.
13. Encode SNSLike and Stuck over shared trajectory primitives and prove:
   `Stuck -> SNSLike`,
   without assuming the converse.
14. Encode finite models M1, M2, and M3 using unchanged foundational semantics.
15. Run every adversarial/control test in `25h`.
16. Use actual theorem proofs, not booleans standing in for proofs.
17. Use no `sorry`.
18. Run `#print axioms` for every central theorem.
19. Confirm no theorem depends on `sorryAx`.
20. Return the Encoding Decision Log and Formalization Gap Report exactly as required by `25i`.

## Critical non-redesign rule

Do NOT add a stronger premise merely because it makes a theorem easy.

If a theorem cannot be proved from the ratified assumptions:

- leave it unproved;
- explain exactly why;
- identify the missing assumption;
- state whether adding it would strengthen or alter Investigation 25.

Do not silently add it.

Do not replace a proof-relevant relation with a boolean/string convenience representation if the distinction is mathematically load-bearing.

## Particular traps to attack

Actively test for:

- Soul-standing carrier drift;
- self-certified materiality;
- `ReadApp` becoming a semantic oracle;
- R/A/P scalarization;
- R/A/P reconstruction;
- `ModelSufficient` circularity;
- HarmonicOutcome being built into HarmonicAlign;
- fake Fourthness via mere relation count;
- fake interface growth where all modes were already available;
- hindsight contamination in M2;
- meta-awareness with no causal bite;
- Stuck modeled as an unrelated state rather than thresholded SNS;
- ActionEffort conflated with ExcessForcing;
- M3 secretly requiring a global master model;
- duplicated Inquiry-24 law.

## Required output package

Return:

```text
/lean-project/
  lean-toolchain
  lakefile.lean
  Main.lean (or structured source directory)
  Models.lean
  Verification.lean

25_formalization_diff_and_mapping.md
25_encoding_decision_log.md
25_formalization_gap_report.md
25_build_and_axiom_report.md
```

The mapping report must show, section by section, which Lean declarations correspond to which clauses in `25f`.

The build report must include:

- exact Lean version;
- exact commands;
- full successful/failed build status;
- theorem inventory;
- `#print axioms` outputs;
- confirmation regarding `sorryAx`.

## Final verdict

End with exactly one:

```text
FORMALLY-HARDENED
FORMALLY-HARDENED-WITH-PROVISIONAL-ENCODINGS
REQUIRES-RATIFICATION
BLOCKING-CONTRADICTION
```

and explain the smallest concrete reason for that verdict.

Do not broaden the mathematical investigation beyond these files.
