# /tmp Reorganization — Pre-Built Move Commands
## Ready to execute on berton_c approval

### Step 1: Remove duplicate
rm -rf /tmp/metta/soul_impl

### Step 2: Create new dirs
mkdir -p /tmp/nal /tmp/mycelial /tmp/scripts /tmp/explorations

### Step 3: Move files (to be filled with actual file lists from audit)
- /tmp/nal/ — all *nal* files
- /tmp/mycelial/ — all *mycel* files
- /tmp/scripts/ — all *.py files
- /tmp/explorations/ — remaining .md research notes

### Step 4: Update clarity_index.md

Status: Awaiting file list results to finalize move commands.
